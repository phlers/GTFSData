<Window x:Class="GTFSData.AccessibilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        Icon="logon.ico"
        Title="Modul: Erreichbarkeit &amp; Radien" Height="750" Width="1300" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" Background="#121212">

    <Window.Resources>
        <Style TargetType="TextBox" x:Key="SmoothTextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SmoothButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="15,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Margin="0,2,5,2" Padding="12,10">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#252526"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0078D7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#181818" BorderBrush="#222" BorderThickness="0,0,1,0" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="📏 Einzugsbereiche" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,25"/>

                    <TextBlock Text="Radius Bahnhöfe (Meter):" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtTrainRadius" Style="{StaticResource SmoothTextBox}" Text="600" Margin="0,0,0,15"/>

                    <TextBlock Text="Radius Bus / Sonstige (Meter):" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtBusRadius" Style="{StaticResource SmoothTextBox}" Text="300" Margin="0,0,0,20"/>

                    <Border BorderBrush="#333" BorderThickness="0,1,0,0" Margin="0,5,0,20"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="Haltestelle suchen:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtSearch" Grid.Row="1" Style="{StaticResource SmoothTextBox}" Margin="0,20,0,15" TextChanged="TxtSearch_TextChanged"/>

                <ListBox x:Name="ListStops" Grid.Row="2" Background="Transparent" BorderThickness="0" Foreground="White" SelectionChanged="ListStops_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayStopName}" FontWeight="Bold" FontSize="14" Foreground="White" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding stop_desc}" FontSize="11" Foreground="#888" TextTrimming="CharacterEllipsis" Margin="0,4,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="3" Margin="0,20,0,0">
                    <Button Content="🗑️ Alle Radien löschen" Style="{StaticResource SmoothButton}" Background="#DC3545" Click="BtnClear_Click"/>
                    <TextBlock Text="Anleitung: Klicke eine Haltestelle an oder suche sie in der Liste, um den Einzugsbereich zu zeichnen." 
                               Foreground="#666" FontSize="11" Margin="0,12,0,0" TextWrapping="Wrap" FontStyle="Italic" TextAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <gmap:GMapControl x:Name="MainMap" Zoom="10" MinZoom="2" MaxZoom="18" MouseWheelZoomType="MousePositionAndCenter"/>
            <Border VerticalAlignment="Top" HorizontalAlignment="Right" Background="#CC181818" CornerRadius="8" Padding="15,10" Margin="15" BorderBrush="#333" BorderThickness="1">
                <TextBlock x:Name="TxtMapInfo" Text="Zoom näher heran für Haltestellen..." Foreground="White" FontWeight="SemiBold" FontSize="13"/>
            </Border>
        </Grid>
    </Grid>
</Window>